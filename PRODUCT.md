# 禾苗智慧能源平台产品上下文

## 产品类型

面向楼宇运维和能源管理的 Web 产品界面，不是营销网站，也不是纯展示大屏。

## 核心用户

- 楼宇运维值班人员
- 暖通设备工程师
- 能源管理人员
- 研发与策略审批人员

## 核心任务

用户应在进入 Dashboard 后 30 秒内完成三件事：

1. 判断当前系统是否稳定。
2. 找到最需要处理的异常或工单。
3. 决定是否进入设备、诊断、工单或优化页面继续处理。

## 品牌人格

- 专业
- 冷静
- 可信
- 实时
- 异常优先

## 设计原则

- 信息层级优先于装饰效果。
- 每个 Dashboard 模块必须回答一个运营问题。
- 正常信息降低权重，异常和待办提升权重。
- 使用字号、对齐、留白和分隔线建立结构，避免卡片套卡片。
- 图表只保留支持判断的线、区间和标记。
- Mock、示例和模型预测必须明确标识。
- AI 生成的草稿、建议和动作必须经过人工确认。

## 反例

- 营销型 SaaS Hero
- 大面积科技渐变和发光装饰
- 五六张半透明 KPI 小卡嵌套在大卡中
- 每条设备状态都使用胶囊背景
- 大量 10px 或 11px 灰色解释文字
- 同时显示过多图表标记和阈值标签
- 为了“看起来专业”而使用没有计算依据的精确数字

## 运营卡片的产品要求

运营卡片不是普通 CRUD 列表。用户应在数秒内判断对象、风险和下一步动作，但不同业务任务不得被强迫使用同一种行模板。

每个模块必须回答：

1. 当前对象或事件是什么？
2. 为什么需要关注？
3. 当前最重要的数值、流程或风险是什么？
4. 下一步应该进入哪个处理流程？

业务要求：

- 异常与工单：突出未派工、工单编号、流程状态和 SLA；Dashboard 不长期展示完整影响说明，严重未派工项只使用短风险线和红色行动状态。
- 优化机会：使用 01/02/03 建立决策顺序，突出预计日收益和审批状态；置信度保持辅助层级，只有高风险使用红色文字。
- 需关注设备：突出异常原因、点位在线率相对健康线的差值和最后通讯时间；底部使用一句话汇总现场风险。
- 控制偏差：依据偏差与阈值的比例分级，使用“设定值 → 实际值 → 偏差”的参数比较结构，只展示最严重两项。
- 冷源运行矩阵：以系统级判断为主，展示运行组合、关键设备和是否高负荷；删除与标题异常数量重复的顶部统计，正常结论使用中性色。
- 正常对象不占用异常列表行，只通过汇总数量或“运行正常”结论表达。
- 同一业务组可在桌面端等高，但不得通过空白填充制造形式上的对齐；平板和手机由内容自然决定高度。
- 状态表达使用短风险线、必要的圆点、文字和数值层级，避免 Tag、Badge、进度条和胶囊背景堆叠。

## AI Agent

AI 运维助手是平台的统一智能操作层，不是独立聊天机器人。它应贯穿资产、遥测、告警、FDD、工单、优化、能耗、成本和系统治理，形成“发现问题 → 调查证据 → 提出动作 → 人工审批 → 执行 → 验证”的闭环。

产品原则：

- 模型负责理解、规划和解释；业务计算、权限、写入和控制由确定性服务负责。
- 重要结论必须关联真实对象、数据来源、时间范围、计算口径和不确定性。
- AI 默认只读；业务写操作必须经过明确确认，高风险控制必须经过更高等级审批、灰度和回滚。
- 全局抽屉、`/ai` 工作台和业务页面内联入口共享会话、任务、对象上下文和权限快照。
- 执行动作成功不等于问题解决，必须在观察窗口内验证告警、诊断、设备指标和能源效果。

完整产品与技术规范见 `docs/ai-agent-product-technical-design.md`。

## 综合能耗分析系统

能耗分析不是单张报表页，而是共享同一建筑、能源类型、周期和对比口径的四种分析工作台。侧边栏只保留一个“能耗分析”入口，页面内部按时间尺度切换：

- 年度分析：服务经营复盘、年度目标、费用、碳排、建筑绩效和长期能效判断。
- 月度分析：服务 MTD 绩效、异常日期、设备类别和具体设备定位。
- 周度分析：服务工作日/周末模式、非营业时段、启停日程和策略调整。
- 日度分析：服务小时负荷、峰平谷、设备启停、FDD、告警和优化处置。

跨周期必须形成连续钻取，而不是四套孤立页面：

```text
年度异常月份
→ 月度异常日期与类别
→ 日度小时负荷和设备
→ 资产 / FDD / 优化建议

周度异常日期
→ 日度运行与业务事件
```

统一路由为 `/energy/year`、`/energy/month`、`/energy/week`、`/energy/day`。年份、月份、周、日期、能源类型、对比口径、设备类别和设备 ID 必须保存在 URL 中，刷新、分享和浏览器前进后退均应恢复当前分析上下文。

一期仍以暖通电能为真实交互范围；水耗、燃气和冷量在信息架构和数据模型中预留，但未接入计量接口前必须显示“待接入”，不得生成伪实际数据。
