# 泉来禾智慧能源平台产品上下文

## 1. 文档职责

`PRODUCT.md` 只记录稳定的产品事实、用户任务、业务语义、安全边界和跨页面工作流。

它不规定具体视觉风格、颜色、圆角、卡片形态、页面几何、滚动模型、Drawer/Sheet/Splitter 等 UI 实现。上述内容属于 `DESIGN.md` 和具体 Surface 的设计决策。

当前实现、旧参考图和旧组件均不能反向定义产品事实。

## 2. 产品类型

泉来禾智慧能源平台是面向楼宇运维、暖通运行和能源管理的 Web 操作产品，覆盖实时运行、异常处置、设备、告警、工单、控制、诊断、优化、能耗和系统治理。

它不是营销网站，也不是只用于展示的大屏。

## 3. 核心用户

- 楼宇运维值班人员。
- 暖通设备工程师。
- 能源管理人员。
- 研发、策略和控制审批人员。
- 平台与系统治理人员。

## 4. 核心任务

### 4.1 全局任务链

平台应支持用户完成连续闭环：

```text
理解当前运行状态
→ 发现值得关注的问题
→ 定位相关对象
→ 查看可信证据
→ 判断原因与影响
→ 决策 / 审批
→ 执行动作
→ 验证结果
```

### 4.2 运营判断与任务分流

用户进入产品后，应能快速回答：

1. 当前系统运行是否稳定。
2. 哪个异常、告警、工单、效率问题或节能机会最值得先处理。
3. 下一步应该进入哪个业务工作流继续处理。

这个职责是运营判断与任务分流，不是把所有系统数据同时展示出来。

### 4.3 实时运行与工程调查

实时运行工作区应帮助用户：

1. 看清当前 HVAC 系统正在如何运行。
2. 识别异常设备、异常参数或异常关系。
3. 在不丢失站点 / 建筑 / 系统上下文的前提下查看设备和证据。
4. 进入趋势、告警、诊断、工单、控制或策略工作流。
5. 在执行后验证真实结果。

### 4.4 资产与设备调查

资产工作流不是 Registry CRUD。它服务于运营人员对设备群体的扫描、筛选、比较和调查。

用户需要能够：

- 看清当前设备范围与空间上下文。
- 找到异常或需要关注的设备。
- 比较设备的运行、通信、数据和诊断事实。
- 查看单个设备的当前值、历史、告警、维护和控制上下文。
- 在需要持续调查时进入可分享、可恢复的完整设备上下文。

### 4.5 异常、诊断与处置

异常工作流服务于发现、调查和处置，而不是仅展示事件记录。

用户需要能够：

- 判断严重程度和当前生命周期。
- 识别来源对象和影响上下文。
- 查看时间、证据、关联设备、诊断结论、工单和责任人。
- 完成确认、分派、转工单和调查等被授权流程。
- 把“告警事实”“诊断推断”“人工责任”和“物理恢复”保持为独立语义。

### 4.6 责任与现场执行

工单工作流应围绕责任、状态、SLA、现场动作、证据和验证结果组织，并能从告警、设备、诊断或优化机会进入。

### 4.7 能源、效率与优化

能源工作流不是单张报表页，而是共享建筑、能源类型、时间范围和比较口径的连续分析流程。

用户需要能够：

- 判断能源用在哪里、何时出现异常和变化来自哪里。
- 分析系统、主机、输配和末端的真实效率事实。
- 从异常用能或低效率现象继续定位到设备、运行模式、事件和控制动作。
- 识别可验证的节能机会，并区分“候选建议”“已审批方案”“已执行动作”和“已验证收益”。

分析时间尺度可以覆盖年度、月度、周度、日度和更细粒度；可分享、可恢复的分析上下文应进入 URL，但具体路由结构不属于产品事实，由当前信息架构决定。

一期仍以暖通电能为真实交互范围；水耗、燃气和冷量在没有真实接口前必须标识为未接入，不得生成伪实际数据。

## 5. 产品语义与真实性

### 5.1 不伪造事实

不得为了构图、演示或视觉完整而伪造：

- 设备状态。
- 告警。
- FDD / 诊断。
- 遥测值。
- 数据质量。
- 权限。
- 控制结果。
- 能流分配。
- 节能量或收益。
- 模型置信度。

Mock、示例、模拟和模型输出必须明确标识。

### 5.2 未知不是零

`未知 / 不可用 / 未接入 / 缺失` 不得表现为 `0` 或“正常”。

### 5.3 独立状态维度

以下事实必须保持独立，不能合并成一个模糊的“健康状态”：

- Runtime State。
- Connectivity / Presence。
- Telemetry Freshness。
- Telemetry Quality / Readiness。
- Diagnosis / FDD。
- Alarm State / Severity。
- Maintenance State。
- Control / Command State。

因此：

- Offline 不自动等于 Fault。
- Stale 不自动等于 Alarm。
- 数据质量问题不自动等于诊断故障。
- 实时平台连接中断不自动等于设备离线。

### 5.4 操作员可见信息

生产 UI 优先展示完成当前任务所需的业务事实：

- 对象。
- 状态。
- 影响。
- 时间。
- 原因 / 证据。
- 责任。
- 下一步可执行动作。

内部追踪和实现字段，例如 UUID、trace/correlation ID、resourceId、actorId、sourceReference、schema/contract 名称、revision/digest、内部节点 ID、技术枚举和值对象版本，不作为主要操作界面内容。

当内部值具有业务意义时，应翻译为用户能理解的业务语言；不得通过截断或装饰内部 UUID 伪造业务编号。

## 6. 时间、数据与上下文

- 后端传输和持久化时间使用 UTC。
- 业务日、结算周期、峰平谷等按 Site Timezone 解释。
- 可分享、可恢复、需要浏览器前进后退恢复的上下文进入 URL。
- 服务端事实由服务端数据状态所有者维护，不复制为第二份客户端真相。
- 实时页面遵循 Snapshot + Stream；Snapshot 与实时增量共同形成当前可见状态。

## 7. 控制安全

远程控制不是普通 CRUD 写操作。

只有在真实前置条件满足时才允许进入执行，包括但不限于：

- 用户拥有相应权限。
- 当前对象允许控制。
- 设备在线 / 状态同步满足真实控制要求。
- 联锁和安全条件满足。
- 用户清楚知道对象、当前状态、目标状态、参数变化和影响范围。
- 高风险操作完成所需审批和确认。

控制生命周期需要区分请求、发送、设备确认、执行结果和结果验证。

`ACK` 不等于目标状态已经实现。

超时或结果未知时，不得描述为“已确认失败”；需要通过状态回读、遥测或其他真实证据继续解析结果。

## 8. AI Agent

AI 运维助手是平台的统一智能操作层，不是脱离业务上下文的独立聊天机器人。

它应贯穿资产、遥测、告警、FDD、工单、优化、能耗、成本和系统治理，帮助形成：

```text
发现问题
→ 调查证据
→ 提出解释 / 建议
→ 人工审批
→ 执行
→ 验证
```

产品原则：

- 模型负责理解、规划、解释和生成候选方案；确定性业务计算、权限、写入和控制由业务服务负责。
- 重要结论应关联真实对象、数据来源、时间范围、计算口径和不确定性。
- AI 默认只读；业务写操作必须经过明确确认，高风险控制遵守更高等级审批、灰度和回滚要求。
- AI 会话、任务、对象上下文和权限快照应能在相关业务入口之间连续传递。
- 动作执行成功不等于问题解决，必须在观察窗口内验证告警、诊断、设备指标和能源效果。

## 9. 产品设计不变量

无论未来采用哪一套视觉设计，都必须满足：

1. 业务真实性优先于视觉完整。
2. 异常、风险和待办比正常背景信息更容易被发现。
3. 同一个业务事实只有一个明确语义，不因页面不同而改变含义。
4. 用户可以从摘要进入证据，再进入行动与验证，而不是被迫在多个孤立页面重新建立上下文。
5. 控制与其他高风险动作必须保持清晰的权限、安全和确认边界。
6. 旧页面、旧参考图、旧 CSS 和旧组件没有设计上的既得权。

## 10. 与设计文档的边界

`PRODUCT.md` 不决定以下问题：

- 左侧导航还是顶部导航。
- Header 是否存在以及多高。
- Dashboard 是否使用卡片、表格、画布或其他构图。
- 页面是否固定视口或 document 滚动。
- Device Center 是否使用卡片墙、表格、树、分栏或其他布局。
- 临时详情使用 Sheet、Inspector、Split Pane、Popover 还是其他模式。
- KPI 是否使用 Metric Strip。
- 颜色、字体、字号、间距、圆角、阴影和 Surface 体系。
- HVAC Monitor 的具体视觉构图。

这些问题由新的设计方向和 `DESIGN.md` 决定。